"# 左側リボンメニュー追加の実装計画\n\n左端にスリムなリボンメニューを追加し、サイドバーの開閉、表示ビューの切り替え、設定ダイアログの呼び出しなどを直感的に行えるようにUIを改良します。\n\n## ユーザー確認事項\n\n> [!NOTE]\n> **デザインとレイアウトの変更点**\n> * 画面左端に幅約 `w-16` (64px) のダークトーンのリボンメニューを追加します。\n> * リボンメニューには「ファイル一覧 (Explorer)」「AIチャット (Chat)」「GitHub同期 (Sync)」「設定 (Settings)」のアイコンを配置します。\n> * 「ファイル一覧」アイコンをクリックすると、現在左側にあるノートリスト（サブサイドバー）の開閉（トグル）が行えます。\n> * 「AIチャット」アイコンをクリックすると、右側にあるAIチャットパネルの表示・非表示をトグルします。\n> * 設定ボタンはリボンメニューの最下部に固定配置し、すぐにアクセス可能にします。\n\n## 提案される変更内容\n\n### [ノートアプリ UI]\n\n#### [MODIFY] [App.tsx](file:///c:/Users/user/OneDrive/Desktop/Ai%E3%83%81%E3%83%A3%E3%83%83%E3%83%88%E5%88%B6%E4%BD%9C/src/renderer/src/App.tsx)\n\n* **状態管理の追加**:\n  ```typescript\n  const [isLeftSidebarOpen, setIsLeftSidebarOpen] = useState(true);\n  const [isRightSidebarOpen, setIsRightSidebarOpen] = useState(true);\n  const [activeTab, setActiveTab] = useState<'explorer' | 'search'>('explorer'); // 拡張用\n  ```\n* **HTML/JSX構造の変更**:\n  * メインレイアウト（`flex flex-1`）の先頭に、縦型のリボンメニュー（`<aside className=\"w-16 bg-[#090d1a] border-r border-gray-800/80 flex flex-col items-center py-4 justify-between select-none\">`）を追加します。\n  * 左サイドバー（ノートリスト）を `isLeftSidebarOpen` に基づいて条件付きレンダリング（または幅を `0` にアニメーション
<truncated 892 bytes>